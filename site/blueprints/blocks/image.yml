name: field.blocks.image.name
icon: image
preview: image
fields:
  image:
    label: field.blocks.image.name
    type: files
    query: model.images
    multiple: false
    image:
      back: black
    uploads:
      template: blocks/image
  alt:
    label: field.blocks.image.alt
    type: text
    icon: title

  # bgcolor:
  #   label: Fond sombre
  #   type: toggle
  #   text:
  #     - "non"
  #     - "oui"

  gridcol:
    label: Grille 2 colones ?
    help: permet de placer une image à gauche ou à droite
    type: toggle
    text:
      - "non"
      - "oui"

  position:
    label: Position
    type: select
    options:
      serie-center: Au center
      self-end: A droite
      self-start: A gauche
